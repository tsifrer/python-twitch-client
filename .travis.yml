language: python
python:
    - 2.7
    - 3.4
    - 3.5
    - 3.6
cache: pip
install: 
    - pip install .[test]
before_script:
    - flake8 .
script:
    - python setup.py test --pytest-args "tests/ --cov=./"
after_success:
  - codecov
branches:
  only:
    - master