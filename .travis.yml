language: python
python:
    - 3.5
    - 3.6
    - 3.7
    - 3.8
cache: pip
install: 
    - pip install .[test]
before_script:
    - flake8 .
script:
    - python setup.py test --pytest-args "tests/ --cov=./"
after_success:
  - codecov
branches:
  only:
    - master